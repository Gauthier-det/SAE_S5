export interface Login {
    email: string;
    password: string;
}

export interface Register {
    name: string; // Nom
    last_name: string; // Prénom
    email: string; // Mail
    password: string; // Mdp
}
